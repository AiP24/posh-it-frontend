const startTimer = () => {
  alert("TODO: Timer");
};

const Timer = () => {
  return (
    <button className="btn text timer" onClick={startTimer}>
      Replace This With Timer
    </button>
  );
};

export default Timer;
